
version: '3'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - 8000:8000















# # version: "2"
# # services:
# #   app:
# #     container_name: sample-docker
# #     build:
# #       context: ./
# #       dockerfile: Dockerfile
# #     ports:
# #       - "8000:8000"

